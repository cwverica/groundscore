"use strict";

const db = require("../db");
const { sqlForPartialUpdate } = require("../helpers/sql");
const {
    NotFoundError,
    BadRequestError,
    UnauthorizedError,
} = require("../expressError");

class Location {

    // static async findId(latLngObj)
    // static async createLocation(latLngObj)
    // static async findNearby(city)
    // static calculate distance(lat)



}
