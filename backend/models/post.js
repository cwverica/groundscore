"use strict";

const db = require("../db");
const { sqlForPartialUpdate } = require("../helpers/sql");
const {
    NotFoundError,
    BadRequestError,
    UnauthorizedError,
} = require("../expressError");

class Post {

    // static async findByUser(username)
    // static async findByLocation(locationId)
    // static async createPost(username, locationId, body)
    // static async updatePost(id, body)
    // static async deletePost(id)


}
